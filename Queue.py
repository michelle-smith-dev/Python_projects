# Write a queue, include popleft and pop (which is popright)
# Bonus: Give me a node, delete the node to the right
# Bonus2: I'm going to give you a node and insert the node after (insert_after)
# Bonus3: Give me a node, delete the node out of the list
# Bonus4: Given two nodes, a and b, insert node b, where node a is, and shift everything left

class Node:

    def __init__(self):
        self.head = None

    previous = self.head

    def queue(self, val):
        for i in input1:
            current_val = i
            self.head.next = current_val
            previous = current_val
            print(current_val)


input1 = queue([3, 2, 1])
